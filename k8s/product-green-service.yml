apiVersion: v1
kind: Service
metadata:
  name: product-green
  namespace: ecommerce
spec:
  selector:
    app: product
    version: green
  ports:
    - port: 80
      targetPort: 5678
  type: ClusterIP

